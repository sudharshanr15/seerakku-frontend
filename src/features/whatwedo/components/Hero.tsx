import HeroContainer from '@/components/HeroContainer';

const Hero = () => {
    return(
        <HeroContainer title='WHAT WE DO' desc="At Seerakku, we’ve initiated several impactful programs and projects to create a positive ecological footprint" />
    )
}

export default Hero;